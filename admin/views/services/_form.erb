<% error = @service.errors.include?(:name) %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :name, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :name, :class => 'form-control input-xlarge input-with-feedback', :autofocus => true %>
    <span class='help-inline'><%= error ? f.error_message_on(:name) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @service.errors.include?(:line_id) %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :line_id, :class => 'control-label' %>
  <div class='controls'>
    <%= f.select :line_id, :options => @lines %>' %>
    <span class='help-inline'><%= error ? f.error_message_on(:line_id) : "Ex: a simple text" %></span>
  </div>
</fieldset>
<% error = @service.errors.include?(:increase_constraint) %>
<fieldset class='control-group <%= error ? 'has-error' : ''%>'>
  <%= f.label :increase_constraint, :class => 'control-label' %>
  <div class='controls'>
    <%= f.text_field :increase_constraint, :class => 'form-control input-xlarge input-with-feedback' %>
    <span class='help-inline'><%= error ? f.error_message_on(:increase_constraint) : "Ex: a simple text" %></span>
  </div>
</fieldset>


<div class="form-actions">
  <%= f.submit pat(:save), :class => 'btn btn-primary' %>
  &nbsp;
  <%= f.submit pat(:save_and_continue), :class => 'btn btn-info', :name => 'save_and_continue' %>
  &nbsp;
  <%= link_to pat(:cancel), url(:services, :index), :class => 'btn btn-default' %>
</div>