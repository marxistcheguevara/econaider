<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:services, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:services, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :service, url(:services, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'services/form', :locals => { :f => f } %>
 <% end %>
</div>
