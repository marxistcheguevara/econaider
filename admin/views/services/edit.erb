<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:services, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:services, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:services, :edit, :id => @service.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :service, url(:services, :update, :id => @service.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'services/form', :locals => { :f => f } %>
  <% end %>
</div>
