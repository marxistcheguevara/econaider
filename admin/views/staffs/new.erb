<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:staffs, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:staffs, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :staff, url(:staffs, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'staffs/form', :locals => { :f => f } %>
 <% end %>
</div>
