<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:needs, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:needs, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :need, url(:needs, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'needs/form', :locals => { :f => f } %>
 <% end %>
</div>
