<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:lines, :index) %></li>
  <li> <%= link_to tag_icon(:plus, pat(:new)), url(:lines, :new) %></li>
  <li class="active"> <%= link_to tag_icon(:edit, pat(:edit)), url(:lines, :edit, :id => @line.id) %></li>
</ul>
<div class="tabs-content">
  <% form_for :line, url(:lines, :update, :id => @line.id), :method => :put, :class => 'form-horizontal' do |f| %>
    <%= partial 'lines/form', :locals => { :f => f } %>
  <% end %>
</div>
